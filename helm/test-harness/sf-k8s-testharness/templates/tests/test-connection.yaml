apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sf-k8s-testharness.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "sf-k8s-testharness.name" . }}
    helm.sh/chart: {{ include "sf-k8s-testharness.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ .Values.name }}-0:{{ .Values.service.port }}']
  restartPolicy: Never
